{
  "name": "dietoken",
  "version": "0.1.0",
  "description": "Kill wasted tokens. Keep better context.",
  "type": "module",
  "bin": {
    "dietoken": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "README.pt-BR.md",
    "docs"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run build && npm test"
  },
  "keywords": [
    "codex",
    "claude-code",
    "ai-agents",
    "tokens",
    "context",
    "cli"
  ],
  "author": "ThomasTonho",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
